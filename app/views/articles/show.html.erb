<div class="main">
  <div class="container">
    <div class="row">
      <div class="col-md-9">
        <div class="panel">
          <div class="panel-heading">
            <%= @article.title %>
          </div>
          <div class="panel-body">
           <% if @article.content %>
                <%= markdown(@article.content) %>
          <% end %>
          </div>
        </div>
      </div>

      <%= render 'shared/search' %>
    </div>
  </div>
</div>