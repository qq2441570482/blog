%tr
  %td= quote.id
  %td= quote.title
  %td= link_to t('views.crud.edit'), edit_quote_path(quote), class: 'btn btn-sm btn-primary'
  %td= link_to t('views.crud.delete'), quote_path(quote), method: :delete, class: 'btn btn-sm btn-danger'